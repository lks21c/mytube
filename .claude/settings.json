{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "filepath=\"$CLAUDE_FILE_PATH\"; if [ -n \"$filepath\" ] && echo \"$filepath\" | grep -qE '\\.(ts|tsx|js|jsx|mjs)$'; then cd /Users/hydra01/repo/mytube && npx eslint --fix \"$filepath\" 2>/dev/null || true; fi"
          }
        ]
      }
    ]
  }
}
